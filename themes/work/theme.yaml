colors: 
  method: manual 

font_family: JetBrainsMono NFM medium

nvim:
  template_dir: default_configs/nvim

tmux:
  append:
    - from: tmux.conf 
      to: tmux/.tmux.conf 

fastfetch:
  overwrite:
    from: config.jsonrc 
    to: fastfetch/config.jsonc

bash:

